spring:
  profiles:
    active: test


---

spring:
  profiles: test
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: create
    show-sql: true
  datasource:
    driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:10-alpine:///skills_test
    password: postgres
    username: postgres